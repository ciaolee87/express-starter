import timezone from 'moment-timezone';


/*
export class TimezoneUtil {
    static parseToUTC(time: string): Date {
        return timezone.tz(time, 'Etc/UTC').toDate();
    }

    static parse(time: string, timezone: string): Date {
        return timezone.tz(time, timezone).toDate();
    }
}*/
